name: Continuous Integration

on: [push, pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gcc:
    name: GCC
    runs-on: "ubuntu-latest"
    strategy:
      fail-fast: false
      matrix:
        include:
          - gcc: "4.8"
            archive: bionic
          - gcc: "10"
            archive: focal
            cxxflags: -funsigned-char # Issue #1715
          - gcc: "14"
            test: "ppa:ubuntu-toolchain-r/test"
          - gcc: "16"
            test: "ppa:ubuntu-toolchain-r/test"
            m32: true
          - gcc: "16"
            test: "ppa:ubuntu-toolchain-r/test"
            install: lib32gcc1 lib32atomic1 lib32asan8 lib32ubsan1
            cxxflags: -m32
            cflags: -m32
            ldflags: -m32
    steps:
      - name: Workaround for actions/runner-images#9491
        run: sudo sysctl vm.mmap_rnd_bits=28

      - name: Install archived toolchain
        if: matrix.archive
        run: |
          sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 40976EAF437D05B5 3B4FE6ACC0B21F32
          sudo add-apt-repository -yn 'deb http://archive.ubuntu.com/ubuntu/ ${{ matrix.archive }} main'
          sudo add-apt-repository -yn 'deb http://archive.ubuntu.com/ubuntu/ ${{ matrix.archive }} universe'
          sudo apt-get update -y
          sudo apt-get install -y gcc-${{ matrix.gcc }} g++-${{ matrix.gcc }} ${{ matrix.install }}
        timeout-minutes: 5

      - name: Install toolchain test build
        if: matrix.test
        run: |
          sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
          sudo apt-get update -y
          sudo apt-get install -y gcc-${{ matrix.gcc }} g++-${{ matrix.gcc }} ${{ matrix.install }}
        timeout-minutes: 5

      - name: Checkout
        uses: actions/checkout@v6
        timeout-minutes: 1

      - name: Configure
        run: cmake -DCMAKE_BUILD_TYPE=Debug -G Ninja -B build
        env:
          CC: gcc-${{ matrix.gcc }}
          CXX: g++-${{ matrix.gcc }}
          CFLAGS: ${{ matrix.cflags }}
          LDFLAGS: ${{ matrix.ldflags }}
          CXXFLAGS: ${{ matrix.cxxflags }}
        timeout-minutes: 1

      - name: Build
        run: cmake --build build
        timeout-minutes: 10

      - name: Test
        run: ctest --output-on-failure -C Debug --test-dir build
        env:
          UBSAN_OPTIONS: print_stacktrace=1
        timeout-minutes: 2

  clang:
    name: Clang
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Configure
        run: cmake -DCMAKE_BUILD_TYPE=Debug -G Ninja -B build
        env:
          CC: clang
          CXX: clang++

      - name: Build
        run: cmake --build build

      - name: Test
        run: ctest --output-on-failure -C Debug --test-dir build

  arduino:
    name: Build Arduino examples
    runs-on: ubuntu-latest
    needs:
      - gcc
      - clang
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: esp8266
            core: esp8266:esp8266
            board: esp8266:esp8266:d1_mini
            opts: --additional-urls http://arduino.esp8266.com/stable/package_esp8266com_index.json
          - name: esp32
            core: esp32:esp32
            board: esp32:esp32:esp32
            libraries: Ethernet
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - uses: actions/cache@v5
        with:
          key: arduino-${{ runner.os }}-${{ matrix.name }}-${{ hashFiles('**/*.ino', '**/library.properties') }}
          path: |
            ~/.arduino15
            ~/.cache/arduino
            ~/Arduino/libraries

      - name: Install arduino-cli
        run: curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | BINDIR=/usr/local/bin sh

      - name: Install libraries
        if: matrix.libraries
        run: arduino-cli lib install ${{ matrix.libraries }}

      - name: Install core
        run: |
          arduino-cli ${{ matrix.opts }} core update-index
          arduino-cli ${{ matrix.opts }} core install ${{ matrix.core }}

      - name: Build examples
        run: scripts/build-arduino-examples.sh ${{ matrix.board }}
